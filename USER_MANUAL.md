# LumiPath 产品使用说明书

> 产品名称：LumiPath  
> 产品类型：英语词汇学习 Web 应用  
> 文档日期：2026-06-06  
> 适用对象：学生用户、演示人员、项目验收人员

## 1. 产品简介

LumiPath 是一款面向英语词汇学习的网页应用，主要帮助用户完成每日背词、词义练习、限时挑战、错题复习和学习记录查看。

应用支持用户注册登录，每个用户的打卡记录、已学单词、错题本和复习状态都会独立保存。系统会根据用户学习情况优先推荐需要复习的单词和薄弱单词，让背词过程更有针对性。

## 2. 主要功能

| 功能 | 说明 |
| --- | --- |
| 用户账户 | 支持注册、登录、退出，学习记录按用户保存 |
| 游客体验 | 未登录时可查看产品首页和随机体验词卡 |
| 每日背词 | 每日抽取 10 个单词，完成后自动打卡 |
| 词义练习 | 支持“看中文选英文”和“看英文选中文”两种模式 |
| 挑战模式 | 支持 5 个难度等级，包含倒计时、血量和连击 |
| 错题本 | 自动收集答错或超时的单词，可手动移出 |
| 学习档案 | 查看打卡次数、累计学习单词数、连续打卡天数和日历 |
| 个性化复习 | 根据用户学习状态优先推荐到期复习词和薄弱词 |

## 3. 运行环境

### 3.1 推荐环境

- 操作系统：Windows 10 / Windows 11
- 浏览器：Chrome、Edge 或其他现代浏览器
- Node.js：项目依赖 npm 脚本启动
- Python：用于启动 FastAPI 后端

### 3.2 项目目录

项目位于：

```text
C:\Users\25461\Documents\LumiPath
```

## 4. 启动产品

### 4.1 启动前后端服务

在项目目录下运行：

```bash
npm run dev
```

该命令会同时启动：

- 前端页面服务。
- 后端 API 服务。

默认访问地址：

```text
http://127.0.0.1:5317
```

### 4.2 单独启动前端

```bash
npm run dev:web
```

### 4.3 单独启动后端

```bash
npm run dev:api
```

### 4.4 停止服务

在运行服务的终端中按：

```text
Ctrl + C
```

也可以使用项目中的停止脚本：

```text
stop-lumipath.cmd
```

## 5. 首次使用流程

### 5.1 打开产品

1. 确认服务已经启动。
2. 打开浏览器。
3. 访问 `http://127.0.0.1:5317`。

进入页面后，如果尚未登录，会看到 LumiPath 的游客首页。

### 5.2 游客状态

未登录时可以浏览首页，并查看随机展示的体验词卡。游客状态下不会保存完整学习记录。

如需开始正式学习，请点击页面中的登录入口。

### 5.3 注册账户

1. 点击登录入口，打开登录弹窗。
2. 切换到“注册”选项。
3. 输入账户昵称。
4. 输入至少 4 位密码。
5. 再次输入确认密码。
6. 点击“创建并进入”。

注册成功后会自动进入学习中心。

### 5.4 登录账户

1. 打开登录弹窗。
2. 保持在“登录”选项。
3. 输入账户昵称。
4. 输入账户密码。
5. 点击“登录学习中心”。

登录成功后，系统会自动加载该账户的打卡记录和错题本。

## 6. 学习中心说明

登录后会进入学习中心。学习中心采用轮盘式入口，主要包含以下区域：

- 每日背词。
- 练习。
- 挑战。
- 错题本。

在桌面端，鼠标移入轮盘区域后功能入口会展开；在移动端，功能入口会以纵向布局展示。

## 7. 每日背词使用方法

### 7.1 开始每日背词

1. 登录账户。
2. 在学习中心点击“每日背词”。
3. 系统会抽取 10 个单词进入学习弹窗。

### 7.2 背词步骤

每个单词会先展示：

- 英文单词。
- 英式音标。
- 美式音标。
- 词性。
- CEFR 等级。
- 难度标签。

使用建议：

1. 先看英文单词，自己回忆中文意思。
2. 点击“查看答案”。
3. 阅读中文释义、英文解释、例句和记忆提示。
4. 确认掌握后点击“已掌握，下一个”。

### 7.3 完成并打卡

当 10 个单词全部学习完成后，按钮会变为“完成并打卡”。

点击后系统会：

- 记录当天打卡。
- 保存本次学习过的单词。
- 更新该用户的学习状态。
- 自动打开学习档案面板。

### 7.4 重新抽词

背词弹窗顶部有重新抽词按钮。如果当前这组单词不合适，可以点击该按钮重新获取一组单词。

注意：重新抽词会回到本组第 1 个单词。

## 8. 词义练习使用方法

### 8.1 进入练习

1. 在学习中心找到“练习”入口。
2. 展开练习选项。
3. 选择一种练习模式：
   - 看中文选英文。
   - 看英文选中文。

### 8.2 看中文选英文

该模式会显示中文释义，用户需要从多个英文选项中选择正确单词。

适合训练：

- 中文释义到英文单词的反应速度。
- 单词拼写识别。
- 词义匹配能力。

### 8.3 看英文选中文

该模式会显示英文单词，用户需要选择正确中文释义。

适合训练：

- 英文单词识别。
- 中文释义理解。
- 阅读理解中的词义反应。

### 8.4 答题规则

每轮练习共 10 题。

每题操作流程：

1. 查看题目。
2. 点击一个答案选项。
3. 系统立即显示答题反馈。
4. 点击“下一题”进入下一题。

答对时：

- 正确数增加。
- 系统记录一次正常复习结果。

答错时：

- 系统显示正确答案。
- 系统显示用户选择的错误答案。
- 该单词自动加入错题本。

### 8.5 查看成绩

完成 10 题后，系统会展示：

- 正确率。
- 答对题数。
- 表现评价。

用户可以选择关闭，也可以点击“再练一组”继续练习。

## 9. 挑战模式使用方法

挑战模式是更游戏化的词义训练方式，包含倒计时、血量、连击和难度等级。

### 9.1 进入挑战

1. 在学习中心找到“挑战”入口。
2. 展开挑战难度。
3. 选择一个难度等级。

### 9.2 难度说明

| 难度 | 名称 | 初始血量 | 每题限时 |
| --- | --- | --- | --- |
| 1 | 轻松 | 10 HP | 30 秒 |
| 2 | 标准 | 8 HP | 25 秒 |
| 3 | 进阶 | 6 HP | 20 秒 |
| 4 | 高压 | 5 HP | 15 秒 |
| 5 | 极限 | 3 HP | 10 秒 |

建议新用户先从“轻松”或“标准”开始。

### 9.3 挑战规则

每题会展示：

- 英文单词。
- 英文解释。
- 多个中文释义选项。
- 当前剩余时间。
- 当前血量。
- 当前连击数。

用户需要在倒计时结束前选择正确中文释义。

答对时：

- 答对题数增加。
- 连击数增加。
- 进入下一题。

答错时：

- 扣除血量。
- 连击清零。
- 单词加入错题本。

超时时：

- 视为答错。
- 扣除血量。
- 单词加入错题本，并标记为超时。

### 9.4 挑战结束

当血量归零时，挑战结束。

结束页会显示：

- 最终答对题数。
- 重新挑战按钮。
- 关闭按钮。

用户可以点击“重新挑战”再次使用当前难度开始。

## 10. 错题本使用方法

### 10.1 打开错题本

在学习中心点击“错题本”入口即可打开。

### 10.2 错题来源

以下情况会自动加入错题本：

- 词义练习中答错。
- 挑战模式中答错。
- 挑战模式中超时。

### 10.3 错题展示内容

每条错题会展示：

- 排名序号。
- 单词。
- 英文解释。
- 中文释义。
- CEFR 等级。
- 出错模式。
- 错误次数。
- 上次误选内容。
- 连续练对进度。

错题会按错误次数和最近出错时间排序，错误更多、最近出错的单词会更靠前。

### 10.4 移出错题

错题可以通过两种方式移出：

1. 手动点击错题右侧的“移出”按钮。
2. 在练习中连续练对该单词 3 次后，系统自动移出。

## 11. 学习档案使用方法

### 11.1 打开学习档案

登录后可通过顶部用户入口打开学习档案。完成每日背词打卡后，系统也会自动打开学习档案。

### 11.2 档案内容

学习档案展示：

- 用户昵称。
- 打卡记录次数。
- 累计背过单词数。
- 当前连续打卡天数。
- 当前月份打卡日历。

### 11.3 打卡日历说明

日历中：

- 当天日期会被特殊标记。
- 已打卡日期会高亮显示。
- 非当前月份日期会弱化显示。

用户可以通过日历快速查看本月学习连续性。

### 11.4 退出登录

在学习档案中点击“退出登录”即可退出当前账户。

退出后：

- 页面回到游客状态。
- 当前用户 token 会从本地浏览器中清除。
- 下次使用需要重新登录。

## 12. 学习数据保存说明

LumiPath 会保存以下学习数据：

- 用户账户。
- 登录会话。
- 每日打卡记录。
- 已学单词记录。
- 错题本记录。
- 单词复习状态。

默认数据库文件位于：

```text
data/lumipath.sqlite
```

浏览器本地也会保存少量登录和兜底数据，例如：

- 当前用户昵称。
- 当前登录 token。
- 本地打卡缓存。
- 本地错题缓存。

## 13. 推荐学习流程

建议每天按以下顺序使用：

1. 登录账户。
2. 完成“每日背词”并打卡。
3. 进入“练习”，做一轮看中文选英文。
4. 再做一轮看英文选中文。
5. 打开“错题本”，查看高频错误单词。
6. 进入“挑战模式”，选择合适难度巩固。
7. 打开“学习档案”，确认当天打卡完成。

## 14. 常见问题

### 14.1 为什么需要登录？

登录后系统才能保存个人学习进度、打卡记录和错题本。如果不登录，只能浏览游客首页，无法形成完整学习记录。

### 14.2 密码有什么要求？

密码至少 4 位。注册时需要输入两次密码进行确认。

### 14.3 每天可以打卡多次吗？

系统按日期记录打卡。同一天再次提交时，会更新当天记录，而不是新增另一条同日期记录。

### 14.4 每日背词为什么有 10 个单词？

当前产品设定每日学习数量为 10 个，便于用户保持稳定学习节奏。

### 14.5 练习模式一轮有多少题？

当前每轮练习为 10 题。

### 14.6 错题什么时候会消失？

用户可以手动移出错题；也可以在练习中连续练对该单词 3 次，系统会自动将它移出错题本。

### 14.7 挑战模式为什么会扣血？

挑战模式中答错或超时都会扣血。不同难度有不同初始血量和限时时间。

### 14.8 后端服务没启动会怎样？

如果后端服务没有启动，登录、保存打卡和错题等功能可能无法正常使用。部分前端功能有本地兜底逻辑，但正式使用时建议保持后端运行。

### 14.9 忘记密码怎么办？

当前版本暂未提供找回密码功能。演示或测试时可以新建一个账户继续使用。

### 14.10 为什么有些中文显示不正常？

如果词库或接口中的部分中文出现乱码，通常是编码问题导致。建议统一使用 UTF-8 编码保存和读取项目文件。

## 15. 注意事项

- 使用前请先启动服务。
- 正式学习前请先注册或登录账户。
- 不建议直接删除 `data/lumipath.sqlite`，否则会清空学习数据。
- 如果修改了端口配置，请按实际端口访问页面。
- 如果浏览器缓存了旧 token，登录异常时可以退出后重新登录。

## 16. 版本状态

当前版本已经具备完整学习闭环：

- 能注册登录。
- 能每日背词。
- 能完成打卡。
- 能做双向练习。
- 能进行限时挑战。
- 能自动记录错题。
- 能查看学习档案。

后续可继续扩展发音、词库搜索、收藏词、学习趋势图、导出学习记录和密码找回等功能。
