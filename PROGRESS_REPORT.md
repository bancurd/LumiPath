# LumiPath 项目进展报告

> 报告日期：2026-06-03  
> 项目定位：LumiPath 是一个面向英语词汇学习的 Web 应用，提供每日背词、练习测验、挑战模式、错题本、学习档案和打卡统计等功能。

## 一、项目整体进展

目前项目已经完成了一个可运行的前后端一体化学习应用原型。前端基于 React、TypeScript 和 Vite 构建，后端基于 FastAPI 提供接口服务，学习数据持久化到 SQLite 数据库中。

当前已完成内容包括：

- 用户注册、登录、退出和会话保存。
- 游客首页和登录后学习中心两套主界面。
- 每日背词流程，每次学习 10 个单词，完成后自动记录当日打卡。
- 双向词义测验，包括“看中文选英文”和“看英文选中文”。
- 限时挑战模式，支持 5 个难度等级、血量、计时、连击和结束页。
- 错题本功能，自动记录练习或挑战中的错误单词。
- 错题连续练对后自动移出机制。
- 学习档案面板，展示打卡次数、背过单词数、连续打卡天数和月历。
- 基于用户学习状态的 SRS 选词策略。
- 本地启动脚本和生产构建流程。

## 二、技术栈与项目结构

### 1. 前端技术

前端使用 React 18、TypeScript、Vite 和 lucide-react 图标库实现。主要代码位于：

- `src/App.tsx`：应用主逻辑和主要界面组件。
- `src/styles.css`：整体视觉样式、响应式布局、弹窗、学习轮盘、挑战模式等样式。
- `src/lib/animal-island-ui.tsx`：封装通用 UI 组件，如按钮、卡片、徽章、日历图标等。
- `src/data/vocabulary.json`：词库数据，目前包含 200 个词条。

### 2. 后端技术

后端使用 FastAPI 和 SQLite。主要代码位于：

- `server/main.py`：接口、认证、数据库初始化、学习记录、错题记录和 SRS 逻辑。
- `data/lumipath.sqlite`：运行时生成的 SQLite 数据库文件。

### 3. 启动与构建脚本

项目已配置 npm 脚本：

- `npm run dev`：同时启动前端 Vite 服务和 FastAPI 后端服务。
- `npm run dev:web`：单独启动前端。
- `npm run dev:api`：单独启动后端。
- `npm run build`：执行 TypeScript 编译并生成生产构建。
- `npm run preview`：预览生产构建结果。

辅助脚本包括：

- `scripts/dev.mjs`：并行启动前端和后端，并给日志加上 `api` / `web` 前缀。
- `scripts/run-fastapi.mjs`：查找 Python 运行时并启动 uvicorn。
- `start-lumipath.cmd` / `stop-lumipath.cmd`：Windows 下的启动和停止入口。

## 三、已完成功能与实现方式

### 1. 用户账户系统

已完成注册、登录、退出和会话保持。

实现方式：

- 前端在登录弹窗中提供“登录 / 注册”切换。
- 注册和登录分别调用 `/api/auth/register` 与 `/api/auth/login`。
- 后端使用 `hashlib.scrypt` 对密码加盐哈希，避免明文保存密码。
- 登录成功后后端生成 session token，有效期为 30 天。
- 前端将昵称和 token 保存到 `localStorage`，刷新页面后可继续保持登录状态。
- 需要用户身份的接口统一通过 `Authorization: Bearer <token>` 传递身份信息。

### 2. 游客首页与登录后学习中心

已完成未登录和已登录两种状态的界面切换。

实现方式：

- 未登录状态展示产品首页、登录入口和随机体验词卡。
- 已登录状态展示学习轮盘，集中入口包括每日背词、练习、挑战和错题本。
- 前端通过 `isLoggedIn` 判断当前用户状态，并切换 `app-shell--guest` 等样式类。
- 样式通过 `src/styles.css` 中的响应式规则适配桌面和移动端。

### 3. 每日背词与打卡

已完成每日学习流程。用户点击每日背词后会抽取 10 个单词，逐个查看单词、音标、释义、英文解释、例句和记忆提示。完成全部单词后自动记录当天打卡。

实现方式：

- 前端点击“每日背词”后调用 `/api/words/daily?count=10` 获取后端推荐的词 ID。
- 前端通过本地词库 `vocabulary.json` 将词 ID 映射为完整词条。
- 背词弹窗中先隐藏答案，鼓励用户主动回忆，再点击“查看答案”。
- 用户点击“已掌握，下一个”进入下一词。
- 完成后调用 `/api/checkins/today` 写入当天打卡和本次学习词 ID。
- 后端同步更新 `checkins`、`learned_words` 和 `user_word_state` 表。
- 如果后端请求失败，前端会回退到本地随机抽词和 `localStorage` 保存，保证基本体验不中断。

### 4. 学习档案与打卡日历

已完成个人学习档案弹窗。

实现方式：

- 前端从 `/api/checkins` 拉取历史打卡记录。
- 根据打卡记录计算：
  - 总打卡次数。
  - 累计背过单词数。
  - 当前连续打卡天数。
- 使用 `buildMonthDays` 生成当前月份日历格子。
- 已打卡日期会在日历中高亮，并显示图标标记。
- 档案面板同时提供退出登录按钮。

### 5. 双向词义练习

已完成两种练习模式：

- 看中文，选英文。
- 看英文，选中文。

实现方式：

- 用户从学习轮盘进入练习子菜单后选择模式。
- 前端调用 `/api/words/practice?count=10` 获取练习词。
- 每题由 1 个正确答案和 3 个随机干扰项组成。
- 答题后立即展示正确 / 错误反馈，并显示正确答案。
- 答对时调用 `/api/words/review-result` 记录 `good`。
- 答错时调用 `/api/words/review-result` 记录 `again`，并调用 `/api/wrong-answers` 写入错题本。
- 完成全部 10 题后显示成绩、正确率和表现评价，并可再次练习。

### 6. 限时挑战模式

已完成游戏化挑战流程，支持 5 个难度等级。

难度配置包括：

| 难度 | 标签 | 血量 | 每题限时 |
| --- | --- | --- | --- |
| 1 | 轻松 | 10 HP | 30 秒 |
| 2 | 标准 | 8 HP | 25 秒 |
| 3 | 进阶 | 6 HP | 20 秒 |
| 4 | 高压 | 5 HP | 15 秒 |
| 5 | 极限 | 3 HP | 10 秒 |

实现方式：

- 前端通过 `CHALLENGE_LEVELS` 定义各等级血量、扣血和时间限制。
- 每题展示英文单词和英文解释，用户选择中文释义。
- 答对增加分数和连击数。
- 答错或超时扣血，并自动记录到错题本。
- 血量归零后进入挑战结束页，显示最终答对题数。
- 计时器使用 React `useEffect` 维护倒计时，时间归零后自动判定超时。

### 7. 错题本

已完成错题记录、展示、排序、移出和自动毕业机制。

实现方式：

- 练习或挑战中答错的单词会调用 `/api/wrong-answers` 写入后端。
- 后端按 `user_id + word_id` 去重，同一个单词多次答错会增加错误次数。
- 错题记录保存：
  - 单词 ID。
  - 错误次数。
  - 最近错误时间。
  - 出错模式。
  - 上次误选内容。
  - 连续练对次数。
- 前端错题本按错误次数和最近错误时间排序展示。
- 用户可以手动点击“移出”删除错题。
- 在练习模式中同一错题连续练对 3 次后，后端会自动从错题本移除。

### 8. SRS 个性化选词策略

已完成基础间隔复习选词逻辑。

实现方式：

- 后端维护 `user_word_state` 表，记录用户对每个词的学习状态。
- 状态字段包括：
  - 已见次数。
  - 正确次数。
  - 错误次数。
  - 连续答对次数。
  - 记忆难度系数。
  - 间隔天数。
  - 下次复习时间。
  - 当前状态。
- 当用户完成每日背词或练习答题后，会调用 `apply_review_result` 更新状态。
- 后端根据用途选择单词：
  - 每日学习优先选择到期复习词、薄弱词，再补充新词。
  - 练习模式优先选择到期词和薄弱词，提升复习针对性。
- 如果可选词不足，会从完整词库中随机补齐。

### 9. 词库数据

已接入本地词库。

实现方式：

- `src/data/vocabulary.json` 包含 200 个词条。
- 每个词条包含英文单词、词形、音标、词性、中文释义、英文解释、难度、例句、搭配和记忆提示等字段。
- 前端所有学习、练习和挑战展示都基于该词库渲染。
- 后端只返回词 ID，完整词条由前端本地映射，减少接口传输内容。

## 四、后端接口完成情况

当前已实现接口如下：

| 接口 | 方法 | 功能 |
| --- | --- | --- |
| `/api/health` | GET | 后端健康检查 |
| `/api/auth/register` | POST | 注册账户 |
| `/api/auth/login` | POST | 登录账户 |
| `/api/checkins` | GET | 获取用户打卡记录 |
| `/api/checkins/today` | POST | 写入今日打卡 |
| `/api/learned-words` | GET | 获取已学单词记录 |
| `/api/words/daily` | GET | 获取每日学习词 |
| `/api/words/practice` | GET | 获取练习词 |
| `/api/words/review-result` | POST | 写入复习结果并更新 SRS 状态 |
| `/api/wrong-answers` | GET | 获取错题本 |
| `/api/wrong-answers` | POST | 记录错题 |
| `/api/wrong-answers/{word_id}` | DELETE | 删除指定错题 |

## 五、数据库设计完成情况

SQLite 数据库初始化逻辑已完成，后端启动时会自动建表。

已创建的数据表包括：

- `users`：用户基础信息和密码哈希。
- `sessions`：登录 token 和过期时间。
- `checkins`：每日打卡记录。
- `learned_words`：已学单词及曝光次数。
- `wrong_answers`：错题本记录。
- `user_word_state`：SRS 学习状态。

同时已建立常用索引，用于按用户、日期、错题时间和复习到期时间查询。

## 六、界面与交互完成情况

已完成较完整的视觉和交互设计：

- 顶部导航栏和品牌区域。
- 未登录首页的介绍区和体验词卡。
- 登录 / 注册弹窗。
- 学习轮盘主界面。
- 每日背词弹窗。
- 练习测验弹窗和成绩页。
- 挑战模式弹窗、血量条、倒计时、连击和结束页。
- 错题本弹窗。
- 学习档案抽屉和月历。
- 移动端响应式布局。

整体样式通过 CSS 变量统一颜色、字体、圆角、按钮和卡片风格，前端组件复用 `IslandButton`、`IslandCard`、`IslandBadge` 等基础组件减少重复。

## 七、验证结果

已执行生产构建验证：

```bash
npm run build
```

验证结果：

- TypeScript 编译通过。
- Vite 生产构建通过。
- 已成功生成 `dist/index.html`、CSS 和 JS 静态资源。

构建输出摘要：

- CSS 资源约 58.20 kB。
- JS 资源约 352.65 kB。
- 构建耗时约 1.33 秒。

## 八、当前待完善事项

项目主体功能已经可用，但仍有一些可以继续完善的地方：

- 后端部分中文错误信息存在编码显示异常，需要统一修正为正常 UTF-8 中文。
- 词库目前全部是 `basic` 难度，可继续扩充 intermediate / advanced 分层。
- 挑战模式目前主要使用前端随机出题，可以进一步接入后端 SRS 选词策略。
- 目前还没有自动化单元测试和端到端测试，建议补充登录、打卡、错题本和 SRS 相关测试。
- 可以增加学习记录可视化，例如周趋势、正确率趋势和薄弱词分布。
- 可以增加音频发音、收藏词、搜索词库和导出学习记录等功能。

## 九、阶段总结

LumiPath 当前已经从静态页面推进到具备完整学习闭环的应用原型。用户可以注册登录、完成每日背词、参加练习和挑战、记录错题、查看学习档案，并且后端已经具备持久化和个性化复习的基础能力。

从实现角度看，项目已经完成了前端交互、后端接口、数据库表结构、学习状态更新、错题追踪和构建脚本等关键部分。下一阶段重点可以放在数据质量修复、测试覆盖、学习算法优化和更多学习辅助功能上。
